# UCAG-P Project Page

Static project website for UCAG-P and its human-to-robot transfer demonstrations.

The site is deployed from this directory by `.github/workflows/pages.yml` whenever `web-page/` changes. No local build step or server-side runtime is required.

Preview locally:

```powershell
python -m http.server 8770
```